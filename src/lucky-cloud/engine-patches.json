{
 "_doc": "Counted repairs applied to the sha-pinned engine.original.js at BUILD time. The engine file is never edited \u2014 provenance.json pins its hash. build-cloud.py applies these and raises if a count is wrong; contracts.test.cjs applies the same list and asserts the shipped page's engine block is the pinned engine plus EXACTLY these and nothing else. One list, two readers, so the test cannot drift from the build. Adding a repair is one entry here.",
 "patches": [
  {
   "id": "wish-460af7e0-lead-chord-fit",
   "why": "makeLine draws a melodic interval from the style's own table, then _snapChord may only move the note onto a chord tone if the RESULTING interval is also in that table. Instrumented over 200 pieces the snap ran 11,636 times and was blocked 5,144 times, and in 5,132 of those a chord tone was sitting inside its own search radius. The note stayed a semitone off a chord tone to protect an interval statistic. Allowing the landing interval to differ by one semitone from the drawn one keeps the style's shape (_sameClass still holds step-versus-leap and direction) and lets the note land. Measured over 60 seeds: chord tones 45.3% -> 54.5%, semitone clashes 44.0% -> 33.9%, leap rate 0.259 -> 0.256.",
   "old": "    if (ivOk && got !== wantIv && !ivOk[got]) continue;",
   "new": "    if (ivOk && got !== wantIv && !ivOk[got] && Math.abs(got - wantIv) > 1) continue;",
   "count": 1
  },
  {
   "id": "wish-44813890-report-names-its-seek",
   "why": "A position report says which seek set up the engine it reports. The page numbers every seek and stamps that number on the score it sends (lifecycle.js: S.send and cloudTransportMessage). The score rides the handover, through the one-deep queue, into the engine that plays it, so rep.world is the one place that knows which seek a report belongs to. Without it, a report posted before the engine took a seek cannot be told from the first report after it: click the bar that is playing in its first quarter second, and both say a time inside that bar. With two such reports in flight, the MIDI publisher took the old place as the song's last position, the real first report read as the loop coming round, and every note from bar 0 went down the room cables at once: 144-234 note-ons on six real seeds where 25-44 were owed, 6-13 of them twice. One field is added to a message; nothing that renders sound reads it.",
   "old": "    this.post({ type: 'pos', t: rep.clock / this.sr, dur: rep.world.duration,",
   "new": "    this.post({ type: 'pos', t: rep.clock / this.sr, dur: rep.world.duration, cloudSeek: rep.world.cloudSeek,",
   "count": 1
  }
 ]
}
